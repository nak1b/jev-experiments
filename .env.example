# Copy to .env.local and fill in. Never commit .env.local.
# Create a key at https://console.typesafe.ai/settings/keys
# Server-only. Never add the NEXT_PUBLIC_ prefix, which ships the value to the browser.
TYPESAFE_API_KEY=
